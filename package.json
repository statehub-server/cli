{
  "name": "cli",
  "version": "1.0.0",
  "description": "",
  "author": "Olivia Shebang",
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "statehub": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "bundle-core": "rm -rf core-dist && mkdir -p core-dist && cp -r ../core/dist/* ./core-dist",
    "relink": "npm run build && npm run bundle-core && chmod +x dist/index.js && npm link"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/node-windows": "^0.1.6",
    "chalk": "^5.4.1",
    "commander": "^14.0.0",
    "node-linux": "^0.1.7",
    "node-windows": "^1.0.0-beta.8",
    "ora": "^8.2.0",
    "pm2": "^6.0.6"
  }
}
